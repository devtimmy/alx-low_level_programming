#include <stdio.h>
/**
 * main - Print all single digit numbers of base 10 including 0
 *
 * Return: 0 Always Success
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d" num);

	printf("\n");

	return (0);
}
